services:
  - type: worker
    name: slcrp-main-bot
    env: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: python run_bots.py
    envVars:
      - key: DISCORD_BOT_TOKEN
        sync: false
